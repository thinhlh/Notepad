<Window x:Class="MaxLineRTB.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MaxLineRTB"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="Paragraph">
            <Setter Property="Margin" Value="0"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="15*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="12*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="lineNumberTextBox" Grid.Column="0" IsReadOnly="True" VerticalScrollBarVisibility="Auto"/>
        <RichTextBox Grid.Row="0" Grid.Column="1" Name="richTextBox" TextChanged="RichTextBox_TextChanged" VerticalScrollBarVisibility="Auto"/>
        <Button Content="MaxLine" Grid.Row="1" Grid.Column="1" Click="Button_Click"/>
    </Grid>
</Window>
