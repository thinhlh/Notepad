<Window x:Class="Notepad.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:self="clr-namespace:Notepad"
        mc:Ignorable="d"
        Title="Notepad" Height="450" Width="800" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto" WindowStartupLocation="CenterScreen" Icon="SublimeIcon.png">
    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.New"
                        Executed="NewFile_Executed"
                        CanExecute="NewFile_CanExecute"/>

        <CommandBinding Command="ApplicationCommands.Open"
                        Executed="OpenFile_Executed"
                        CanExecute="OpenFile_CanExecute"/>

        <CommandBinding Command="ApplicationCommands.Save"
                        Executed="Save_Executed"
                        CanExecute="Save_CanExecute"/>

        <CommandBinding Command="ApplicationCommands.SaveAs"
                        Executed="SaveAs_Executed"
                        CanExecute="SaveAs_CanExecute"/>


        <CommandBinding Command="self:CustomCommands.CloseFile"
                        Executed="CloseFile_Executed"
                        CanExecute="CloseFile_CanExecute"/>

        <CommandBinding Command="self:CustomCommands.Exit"
                        Executed="Exit_Executed"
                        CanExecute="Exit_CanExecute"/>

        <CommandBinding Command="self:CustomCommands.NewTerminal"
                        Executed="NewTerminal_Executed" 
                        CanExecute="NewTerminal_CanExecute"/>

        <CommandBinding Command="self:CustomCommands.NewTerminalCurrentDir"
                        Executed="NewTerminalCurrentDir_Executed"
                        CanExecute="NewTerminalCurrentDir_CanExecute"/>

    </Window.CommandBindings>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0" Background="White" >
            <MenuItem Header="File" Name="File">
                <MenuItem Header="New File" Command="ApplicationCommands.New"  Name="NewFile"/>
                <MenuItem Header="Open File" Command="ApplicationCommands.Open" Name="OpenFile"/>
                <MenuItem Header="Open Folder" Name="OpenFolder"/>

                <Separator/>

                <MenuItem Header="Save" Name="Save" Command="ApplicationCommands.Save"/>
                <MenuItem Header="Save As" Command="ApplicationCommands.SaveAs" Name="SaveAs"/>
                <MenuItem Header="Save All" Name="SaveAll" Click="SaveAll_Click"/>

                <Separator/>

                <MenuItem Header="Close File" Name="CloseFile" Command="self:CustomCommands.CloseFile"/>
                <MenuItem Header="Close All Files" Name="CloseAllFiles" Click="CloseAllFiles_Click"/>
                <MenuItem Header="New Window" Name="NewWindow" Click="NewWindow_Click"/>

                <Separator/>

                <MenuItem Header="Exit" Name="Exit" Command="self:CustomCommands.Exit"/>
            </MenuItem>

            <MenuItem Header="Edit">
                <MenuItem Header="Cut" Name="Cut" Command="ApplicationCommands.Cut"/>
                <MenuItem Header="Copy" Name="Copy" Command="ApplicationCommands.Copy"/>
                <MenuItem Header="Paste" Name="Paste" Command="ApplicationCommands.Paste"/>
                <MenuItem Header="Undo" Name="Undo" Command="ApplicationCommands.Undo"/>

                <Separator/>

                <MenuItem Header="Find" Name="Find" Command="ApplicationCommands.Find"/>
                <MenuItem Header="Replace" Name="Replace" Command="ApplicationCommands.Replace"/>
            </MenuItem>

            <MenuItem Header="Selection" Name="Selection">
                <MenuItem Header="Select All" Name="SelectAll" Command="ApplicationCommands.SelectAll"/>
            </MenuItem>

            <MenuItem Header="Languages" Name="Languages">
                <ComboBoxItem Name="C" Content="C" />
                <ComboBoxItem Name="CPlusPlus" Content="C++"/>
                <ComboBoxItem Name="Java" Content="Java"/>
                <ComboBoxItem Name="JavaScript" Content="JavaScript"/>
                <ComboBoxItem Name="CSharp" Content="C#"/>
            </MenuItem>

            <MenuItem Header="Run" Name="Run">
                <MenuItem Header="Build" Name="Build" Click="Build_Click" />
                <MenuItem Header="Build And Run" Name="BuildAndRun" Click="BuildAndRun_Click"/>

                <Separator/>

                <MenuItem Header="Debug" Name="Debug"/>
                <MenuItem Header="Run Without Debug" Name="RunWithoutDebug"/>
            </MenuItem>

            <MenuItem Header="Terminal" Name="Terminal">
                <MenuItem Header="New Terminal" Name="NewTerminal" Command="self:CustomCommands.NewTerminal"/>
                <MenuItem Header="Open Terminal in Current Workspace" Name="NewTerminalCurrentDir" Command="self:CustomCommands.NewTerminalCurrentDir"/>
            </MenuItem>

            <MenuItem Header="Preferences" Name="Preferences">
                <MenuItem Header="Dark Mode" Name="Darkmode"/>
                <MenuItem Header="Auto Save" Name="AutoSave"/>
            </MenuItem>

            <MenuItem Header="Help" Name="Help">
                <MenuItem Header="About" Name="About"/>
                <MenuItem Header="Report" Name="Report"/>
            </MenuItem>
        </Menu>

        <TabControl  FontFamily="Consolas" Grid.Row="1" Name="tabControl"/>


        <StatusBar Grid.Row="2" Name="StatusBar" Background="White" BorderThickness="0.3" BorderBrush="Gray" FontWeight="Medium">
            <StatusBarItem>
                <TextBlock Name="StatusText" Text="{Binding}"/>
            </StatusBarItem>
        </StatusBar>
        <Button Click="Test"></Button>
    </Grid>
</Window>