<UserControl x:Class="Notepad.TabItemContentUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Notepad"
             xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
             xmlns:classes="clr-namespace:Notepad.Classes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="25*"/>
        </Grid.ColumnDefinitions>

        <WindowsFormsHost Grid.Column="0">
            <wf:TextBox x:Name="textBox" BorderStyle="None" Multiline="True" WordWrap="True" ReadOnly="True" HideSelection="True" IsAccessible="False"/>
        </WindowsFormsHost>
        <!--<TextBox Grid.Column="0" x:Name="textBox" Text="{Binding LineNumber}" TextAlignment="Left">
            <TextBox.Style>
                <Style TargetType="{x:Type TextBox}" >
                    <Setter Property="IsReadOnly" Value="true"/>
                    <Setter Property="VerticalScrollBarVisibility" Value="Hidden"/>
                    <Setter Property="IsEnabled" Value="False"/>
                    <Setter Property="BorderBrush" Value="Black"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="TextAlignment" Value="Center"/>
                    <Setter Property="BorderThickness" Value="0,0,0.7,0"/>
                    <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
                    <Setter Property="VerticalScrollBarVisibility" Value="Disabled"/>
                </Style>
            </TextBox.Style>
        </TextBox>-->
        <local:RichTextBoxUC Grid.Column="1" x:Name="richTextBoxUserControl" Margin="0,-2,0,0"/>


    </Grid>

</UserControl>
